#ifndef GlobalsH
#define GlobalsH

// Me parece que estas constantes cambian en función de las dimensiones de la pantalla
// el factor de altura se corresponde (creo) con las dimensiones de la pantalla física!!!
// es decir mi monitor tiene 1024 x 600 pues el factor es 1.6

const float WIDTH_CORRECTION    = 2.0f; // 1.6f;
const float HEIGHT_CORRECTION   = 4.0f / 3.0f; // 1.2f;


#endif